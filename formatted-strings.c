#include <stdio.h>

// Prompt user for two strings, separated by a space

int main() {
    char s1[20], s2[20];

    // Give user directions
    printf("");
    // Get user input into 2 strings
    scanf("", s1, s2);
    // Print out strings in reverse order, using width specifier
    printf("");
    return 0;

}